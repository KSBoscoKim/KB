// function add(a, b) {
//   return a + b;
// }

// function toUpperCase(str) {
//     return str.toUpperCase();
// }

// function sumArray(arr) {
//     let sum = 0;
//     for (let i = 0; i < arr.length; i++) {
//         sum += arr[i];
//     }
//     return sum;
// }

let add = function (a, b) {
  return a + b;
};

let toUpperCase = function (str) {
  return str.toUpperCase();
};

let sumArray = function (arr) {
  let sum = 0;
  for (let i = 0; i < arr.length; i++) {
    sum += arr[i];
  }
  return sum;
};

let add2 = (a, b) => {
  return a + b;
};

let toUpperCase2 = (str) => str.toUpperCase();

let sumArray2 = (arr) => {
  let sum = 0;
  for (let i = 0; i < arr.length; i++) {
    sum += arr[i];
  }
  return sum;
};
